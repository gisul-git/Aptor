name: Deploy DSA Service to QA (Debian VM)

on:
  push:
    branches:
      - QA
    paths:
      - 'services/dsa-service/**'
      - '.github/workflows/deploy-qa-dsa-service.yml'
  pull_request:
    types: [closed]
    branches:
      - QA
    paths:
      - 'services/dsa-service/**'
  workflow_dispatch:

env:
  VM_HOST: ${{ secrets.QA_VM_HOST }}
  VM_USER: ${{ secrets.QA_VM_USER }}
  VM_SSH_KEY: ${{ secrets.QA_VM_SSH_KEY }}
  VM_DEPLOY_PATH: ${{ secrets.QA_VM_DEPLOY_PATH }}

jobs:
  deploy-dsa-service-qa:
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true) ||
      github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: QA

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ env.VM_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ env.VM_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy DSA Service to QA VM
        run: |
          DEPLOY_PATH="${{ env.VM_DEPLOY_PATH }}"
          if [ -z "$DEPLOY_PATH" ]; then
            DEPLOY_PATH="/opt/aptor"
          fi
          
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${{ env.VM_USER }}@${{ env.VM_HOST }} bash << EOF
            set -e
            
            echo "🚀 Starting DSA Service QA deployment..."
            
            cd ${DEPLOY_PATH}
            git fetch origin
            git checkout QA
            git pull origin QA
            
            echo "🔨 Rebuilding DSA Service..."
            docker-compose build dsa-service
            
            echo "🔄 Restarting DSA Service..."
            docker-compose up -d --no-deps dsa-service
            
            sleep 5
            
            if docker-compose ps dsa-service | grep -q "Up"; then
              echo "✅ DSA Service deployed successfully!"
              docker-compose ps dsa-service
            else
              echo "❌ DSA Service deployment failed!"
              docker-compose logs dsa-service
              exit 1
            fi
          EOF

      - name: Verify Deployment
        run: |
          echo "✅ DSA Service deployment completed"

